import ExecutiveStandalonePage from "@/components/ExecutiveStandalonePage";

export default function CroPage() {
  return (
    <ExecutiveStandalonePage
      role="cro"
      title="CRO"
      subtitle="Pipeline, conversion, and the full revenue system — where revenue is created, where it leaks, and what's the rate-limiting step."
    />
  );
}
